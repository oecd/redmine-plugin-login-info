= login_info

This plugin adds a placeholder text in the login box to make it clear what is expected as login. It also checks the "Remain connected" checkbox.

It provides the text in English and French as it should.
